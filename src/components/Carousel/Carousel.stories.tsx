import Carousel, { Props } from './Carousel';

export default { title: 'components/Carousel' };

export const Default = (args: Props) => <Carousel {...args} />;

Default.args = {
  // add control data
};
